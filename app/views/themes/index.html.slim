.theme-list
  - if @themes.present?
    ul
    - @themes.each do |theme|
      li
        .title
          p = theme.title
        .kind
          p = theme.kind
        .content
          p = theme.content
  - else
    | テーマの候補がないよー勉強会しようよー😩😩😩
